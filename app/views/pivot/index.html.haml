.container
  %h2 Basic Table
  %p Sample for only group and brand name, will add options to select columns etc later.
  %table.table.table-striped
    %thead
      %tr
        %th Group Name
        %th Brand Name
        %th Sum of Current year Sales.
        %th Sum of SOH.
        %th Sum of Sales in last 180 days.
        %th Sum of SOH by ROH
        %th Sum of SOH open order ROH
        /You'll do the last one? yes yes

    %tbody
      - @core_tracker_templates.each do |core_tracker_template|
        %tr
          %td
            = core_tracker_template.group_name
          %td
            = core_tracker_template.brand_name
          %td
            = core_tracker_template.sum_cy_saleqty
          %td
            = core_tracker_template.sum_soh
          %td
            = core_tracker_template.sum_last_180days_saleqty

          %td
            - if core_tracker_template.sum_last_180days_saleqty == 0
              = "0"
            - else
              = (core_tracker_template.sum_soh / core_tracker_template.sum_last_180days_saleqty.to_f * 180.00)
          %td
            - if core_tracker_template.sum_last_180days_saleqty.to_f == 0
              = "0"
            - else
              = ((core_tracker_template.sum_soh + core_tracker_template.open_po.to_f ) / (core_tracker_template.last_180days_saleqty.to_f * 180.00))